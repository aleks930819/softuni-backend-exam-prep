<main>

  <section id="details-page">

    <div class="main_card">
      <div class="card_left">
        <div class="card_datails">

          <h1>Name: {{post.name}}</h1>
          <h3>Created by an author: {{post.owner.firstName}} {{ post.owner.lastName }} </h3>
          <div class="card_character">
            <p class="card-species">Species: {{post.species}}</p>
            <p class="card-skin">Skin colour: {{post.skinColor}}</p>
            <p class="card-eye">Eye Colour: {{post.eyeColor}}</p>
          </div>

          <p class="disc">Description: {{post.description}}</p>

          <!-- If there is a registered user -->
          {{#if isLoggedIn}}
          <div class="social-btn">
            <!-- Only for registered user and author of the post -->
            {{#if isOwner}}
            <a href="/posts/edit/{{post._id}}" class="edit-btn">Edit</a>
            <a href="/posts/delete/{{post._id}}" class="del-btn">Delete</a>
            {{/if}}
            <!-- logged in users, who have not yet voted-->
            {{#if canVote}}
            <a href="/posts/vote/{{post._id}}" class="vote-up">Vote</a>
            {{else}}
            <!-- logged in user who has already voted-->
            {{#if hasVoted}}
            <p class="thanks-for-vote">Thanks For Voting</p>
            {{/if}}
            {{/if}}
          </div>
          {{/if}}
        </div>
      </div>
      <div class="card_right">
        <img src="{{post.image}}" alt="image">
      </div>
    </div>

  </section>

  <section id="votes">
    <div class="vote-info">
      <div class="card_left">
        <div class="card_datails">
          <h1>Votes</h1>
          <div class="card_vote">
            <!-- Show the rating of votes -->
            <p class="PV">Total rating of votes: {{post.votes.length}}</p>
          </div>
          <!-- If there are already people who have cast their vote for the post -->
          {{#if post.votes.length}}
          <p class="disc">People who voted for the post - {{#each post.votes}} {{this.email}} {{#unless @last}},
            {{/unless}} {{/each}}</p>
          {{else}}
          <!-- If not display: -->
          <p class="disc">People who voted for the post - No one has voted yet.</p>
          {{/if}}
        </div>
      </div>
    </div>
  </section>

</main>
